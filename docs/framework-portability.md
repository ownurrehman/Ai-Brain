> **Parent Hub:** [[INDEX|🧠 Master Ai Brain Hub]] · **Canonical Document:** [[websites/justccell.com/docs/framework-portability|framework-portability.md]]

# Framework Portability — Cloning & Multi-Site Standards

The canonical framework portability guide is maintained at:
👉 **[[websites/justccell.com/docs/framework-portability|websites/justccell.com/docs/framework-portability.md]]**

### Mandatory Inherited Access Protocol (Rule §0.7)
Every cloned site (e.g., `eliteterpenez.com` and future ecommerce stores) must inherit this exact protocol in its `rules.md`:
> **§0.7 Access: Agents must never attempt GUI logins. All backend modifications must be executed via Hostinger MCP (shared client access). Verify live states via authenticated WP REST API requests using the stored application passwords in the local credentials file (`master-env.env`). Assume direct environment control.**
