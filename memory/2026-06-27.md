# 2026-06-27 — RankRay HQ session

## Supabase-first local dev (B097)
- User reported logged-in but broken UI — root cause: `.env` pointed at dead local Postgres `:5433`, API on `:3010` never booted.
- Switched `rankray-hq-backend/.env` to Supabase pooler (6543) + session pooler (5432 for migrations).
- Perf: `SKIP_DB_MIGRATE=1` on routine `npm run dev`; `start.sh` runs `db:prepare` once; Vite `inspectAttr` opt-in via `VITE_INSPECT=1`.
- Verified: Supabase migrations 4/4 applied; `/health` OK; `dev-login` works.

## Cross-linking plan
- Tiers 1–4 sealed (Phases O–R, B091–B096). 29/29 relational-integrity security tests green.

## Global design cleanup (B099)
- SSOT: `moduleChrome.ts` — `moduleElementStyles`, `moduleDialogChrome`, `legacyModuleCanvas`, `pageHeader`.
- Legacy modules aligned: Dashboard, Analytics, Marketing (full rewrite), Outreach, Inbox, CRM Tasks/CompanyProfile, Projects modals.
- App shell: removed decorative gradient mesh; non-shell padding `p-3`.
- Verified: `npm run build` PASS.

## Design pass 2 + Master Control (B100)
- Dashboard → `ModuleWorkspace` (shell parity with Finance/CRM).
- Automation wizard, MarketingPerformance, FinanceSettings backdrop-blur removed.
- Master Control: `/admin/integrations`, `/admin/features`.
- Expense ledger entries include `companyId` when client-linked.
- Verified: frontend build + backend `tsc -b` PASS.
