# Session log — 2026-06-03 (RankRay HQ)

## Supabase + performance fixes (agent-owned)

- Fixed `/api/auth/me` 500 (tenant + `findUnique`); added 45s `getMe` cache.
- Rebuilt `/api/dashboard/mission-control`: removed full `listAssets()` scan; batched Prisma (3 at a time for PgBouncer `connection_limit=1`); 45s server cache; fixed `SeoAutomationAlert` select fields.
- JWT validate: 60s in-memory user cache (cuts DB round-trips per request).
- Frontend: no full-screen dashboard blocker; 30s GET cache in `api.ts`; clears on login/logout.
- Dev scripts: `dev:backend` 4096MB heap, migrate deploy non-fatal; `dev:all:stable` auto-restart.
- BUG_LEDGER B076 documented.

## Verified (local, port 3010 + Supabase pooler)

- `npx tsc -b` backend PASS; frontend `npm run build` PASS.
- `/api/auth/me` warm ~4ms, cold ~3.4s.
- `/api/dashboard/mission-control` 200 — warm ~1.4s, cold ~18s (remote DB).

## Still slower than SQLite

First cold dashboard load ~15–20s is mostly Sydney round-trips on Supabase; warm/cached loads are usable.

## Next (optional)

- Google OAuth env (`GOOGLE_CLIENT_ID` / `SECRET`) when ready.
- Further cold-path tuning: fewer mission-control queries or edge cache.

## Render + seoengineai.com (deploy prep)

- Added `render.yaml`: `rankray-hq-api` (Starter, Singapore) + `rankray-hq-web` (static).
- Domains: `hq.seoengineai.com` (app), `api.seoengineai.com` (API).
- Guide: `docs/operations/RENDER_DEPLOY.md`; prod env table in `docs/reference/keys.md`.
- Render MCP plugin: `unauthorized`; direct API with key in `~/.cursor/mcp.json` works.
- Pushed `render.yaml` + `RENDER_DEPLOY.md` to `dev-cursor` (commit b2141dd17).
- Render API create blocked **402** — workspace needs billing card before any service can be created.

## Cloudflare tunnel test (no Render)

- Installed `cloudflared`, scripts `npm run tunnel:setup|tunnel:hq|tunnel:quick`, doc `CLOUDFLARE_TUNNEL_TEST.md`.
- Dev running locally; quick tunnel for instant public URL.
- Public test live via quick tunnel (see `.tunnel-public-url`). `hq.seoengineai.com` blocked: no CF API token in repo/MCP; needs `.env` CLOUDFLARE_* or one browser Authorize click.

## B070 — tunnel login + password UI

- Root cause: `VITE_API_URL=http://127.0.0.1:3010/api` used from `*.trycloudflare.com` → Chrome local-network permission + fetch failure.
- Fix: `api.ts` uses same-origin `/api` on tunnel hosts; Login.tsx password eye toggle.
- Files: `rankray-hq-frontend/src/lib/api.ts`, `Login.tsx`, `BUG_LEDGER.md`, `CLOUDFLARE_TUNNEL_TEST.md`.

## Local dev streamline + dev auth bypass (Option A)

- `AUTH_DEV_BYPASS=true` via `npm run dev` — `POST /api/auth/dev-login`, `GET /api/auth/config`; production blocked.
- `rankray-hq-backend/.env.example`, `docs/operations/LOCAL_DEV.md`, primary script `npm run dev` (api+web), `dev:reset` for port clean.
- Login: "Continue as dev admin" button; Google hidden when bypass on.
- Verified: backend + frontend `npm run build` PASS.
