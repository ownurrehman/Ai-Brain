
## OpenClaw update fix (2026-07-14)

- Root cause of `openclaw update` failure: Node too old (nvm `22.22.0` + Homebrew `25.6.1` outside OpenClaw engines `>=22.22.3 <23 || >=24.15.0 <25 || >=25.9.0`).
- Fixed CLI Node: `nvm install 22` → `v22.23.1`, `nvm alias default 22`; reinstalled `openclaw@2026.7.1` under that Node.
- Reinstalled LaunchAgent gateway to use nvm `22.23.1` paths (was pinned to brew node + old `22.22.0` package root).
- Set `gateway.auth.mode=token` (LAN bind refused `auth.mode=none`).
- Gateway still refused ready: conflicting legacy `~/.openclaw/plugins/installs.json` (brave/discord @2026.5.28) vs SQLite index (@2026.7.1). Archived to `installs.json.migrated`.
- Result: gateway listening on `:18789`, health OK, Discord configured, update path unblocked.


## Machine runtime cleanup (2026-07-14 evening)

Simplified AI tooling Node map:
- SSOT shell/OpenClaw Node: nvm `v22.23.1` (forced at end of `~/.zshrc`)
- Homebrew `node` unlinked from PATH (kept in Cellar for `gemini` shebang)
- Hermes gateway reloaded; PATH pin updated `22.22.0` → `22.23.1` (Hermes still Python + `~/.hermes/node`)
- Added `system/MACHINE_RUNTIME.md`, `scripts/ai-runtime-check.sh`, `scripts/ai-runtime-sync.sh`, INDEX links, zsh aliases `ai-runtime-check` / `ai-runtime-sync`
- Leftover nvm `v22.22.0` has root-owned files (could not uninstall cleanly); harmless if zshrc forces default

