# Memory Log — 2026-05-22

## Task: Audit + Relocate GitHub Testing Apps

**Triggered by:** Own (Sheikh Own) — home directory cluttered with forgotten testing apps

**Scope:**
- Audit all GitHub testing/dev applications in `/Users/sheikhown`
- Move to `Ai Brain/applications/` (clean, one-glance view)
- Exclude: `openclaw`, `hermes` (agentic main apps)
- Update INDEX.md with new applications section
- Write applications/README.md as single source of truth

**What was found and moved:**
1. `camofox-browser` → testing (stealth browser)
2. `camofox-tool-backup` → backup
3. `cloakbrowser` → testing (fingerprint evasion)
4. `gitleaks` → testing (secret scanning)
5. `n8n` → testing (workflow automation)
6. `openserp` → testing (SERP scraper)
7. `searxng` → testing (meta search engine)

**What was NOT moved (intentionally):**
- `node_modules` — dependency folder for global packages (not an app)
- `go` — Go SDK (language runtime, not an app)
- `workspace` — empty screenshots folder (not an app)
- `openclaw` / `hermes` — agentic workspaces in Ai Brain root

**Changes made:**
- Created `Ai Brain/applications/.gitignore`
- Created `Ai Brain/applications/README.md` with status table
- Updated `Ai Brain/INDEX.md` — added applications/ to structure + full table
- All apps verified present in target folder, removed from home directory

---

## Task: Delete n8n + camofox-tool-backup

**Decision:** Delete n8n (2.1GB wrapper) and camofox-tool-backup (duplicate)

**Verified before delete:**
- n8n: checked `~/.n8n/` database — 0 workflows, 0 credentials, 0 executions, 1 user, 4 settings. Empty.
- n8n `applications/n8n/` was just a package.json + node_modules wrapper, not real n8n source
- camofox-tool-backup: exact duplicate of camofox-browser repo, just older git history

**Space freed:** ~2.2GB

---

## Task: Full MacBook Space Audit

**Disk:** 460GB total, 16GB used before cleanup

**Biggest space hogs found:**
1. `~/Library/Containers/com.docker.docker/` — 81GB (Docker Desktop VM disk)
2. `~/Downloads` — 17GB
3. `~/Ai Works - Local` — 7.4GB
4. `~/.ollama` — 4.4GB (local LLM models)
5. `~/Library/Caches` — 6.7GB
6. `~/.nvm` — 2.7GB
7. `~/.npm` — 1.6GB

**Docker.raw details:**
- File: `/Users/sheikhown/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw`
- Virtual size: 460GB
- Actual disk usage: 81GB
- Status: Docker not running, no active containers

**Decision:** Option B — delete Docker.raw entirely (nuclear reset)
- User confirmed not running anything on Docker
- Rank Ray HQ may use Docker in future, but will create fresh disk

**Result after delete:**
- `~/Library/Containers/com.docker.docker/` dropped from 81GB to 79MB
- Disk usage: 16GB → 10% capacity (was 18%)
- Available space increased significantly

**Total space freed today:** ~83GB (81GB Docker + 2.2GB apps)

---

**Next step for Own:**
When you want to test something, check `Ai Brain/applications/README.md` first. If it's there, use it. If not, clone it there. Never again into home directory.
