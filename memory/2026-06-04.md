## RankRay HQ — SSOT context pillars (Step 1)

- SSOT pillars now live under **`docs/core/`** (migrated 2026-06-04): `01_prd.md`, `02_architecture_design.md`, `03_data_schema_map.md`, `04_api_integration_contract.md`, `05_state_routing_ledger.md`. Root `.context/` removed — do not recreate.
- Consolidated from `docs/core/*`, `docs/modules/seo/README.md`, `schema.prisma`, `App.tsx`, `appNavigation.ts`, `seoNav.ts`.
- Flagged for Step 2 audit: SEO v2 vs legacy blueprint route drift; SEO.tsx listens only `popstate` not `app:locationchange`; `uiStore.activeSeoTab` legacy.
- User paused before Step 2 system-wide audit — awaiting explicit confirmation.

## RankRay HQ — Cursor skills context trim

- Expanded `.cursor/settings.json`: disabled ~40 marketplace plugins (vercel, elastic, amplitude, shopify, huggingface, searchfit-seo, AWS packs, etc.); kept render, Prisma, cloudflare, cursor-team-kit, superpowers, playwright, firecrawl, postman, stripe.
- Added `scripts/cursor-trim-skills.sh` + `docs/operations/CURSOR_SKILLS_TRIM.md`; linked from `LOCAL_DEV.md`.
- Removed 29 **broken** symlinks from `~/.cursor/skills` (ECC archive path gone). User should Reload Window + new Agent chat to see lower skill token count.

## RankRay HQ — Stage 3 UX unification (module chrome)

- SEO: `src/modules/seo/archive/README.md` — no in-repo legacy views to move; pre-v2 lives in `LEGACY_SEO_BACKUP_TOTAL/`.
- Finance: `FinanceDropZone` + loading shell use `h-full min-h-0` so Option A grid junction aligns with HeaderStrip.
- CRM: "Contact Directory" → "Customers" in `CrmSidebar.tsx` + `CRM_TAB_LABELS`.
- Modals: submit guards on `AddContactModal`, `ContactModal`, `AddDealModal` (in-flight guard), Finance `Items.tsx` create/edit.
- ModuleWorkspace migration: `Assets.tsx`, `Settings.tsx`, `AdminShell.tsx` (removed duplicate ModuleShell wiring).
- Verified: `cd rankray-hq-frontend && npm run build` — pass.

## RankRay HQ — Phases 4–5 deletion safety + seed sanitization (B076)

- **Layout confirmed:** `rankray-hq-frontend/src/components/shared/` — `ModuleWorkspace.tsx`, `moduleChrome.ts`, `HeaderStrip.tsx` (Option A, h-10 strip).
- **Frontend:** `ConfirmDialog` now awaits async confirm, disables buttons, shows `Deleting...`; `useMutationLock` for bulk/inline deletes (Finance Items, CRM/Finance companies, Projects archive).
- **Backend:** `RELATIONAL_DELETE_CONFLICT_MESSAGE` + `HttpExceptionFilter` maps Prisma `P2003`/`P2014` → HTTP 409; company + finance item deletes aligned.
- **CoinSfera:** Removed from E2E (`ApexDigital`); `.context/01` + `03` + `SSOT_LOCK` decouple live client names from dev fixtures (seeds already use Acme demo names).
- **Docs:** `.context/04` (409 protocol), `.context/05` §9.1 transactional constraints, `BUG_LEDGER` B076.
- **Verified:** frontend build PASS, backend `npx tsc -b` PASS, `graphify update .` (6800 nodes).
- **Git:** `64a4118bb` + `36e410b8c` on `dev` pushed to `origin/dev` (deletion safety + graphify sync).

## RankRay HQ — Module canvas density SSOT (B077)

- **`moduleChrome.ts`:** `moduleElementStyles` dictionary (h-8 inputs/buttons, compact cards/stats/tables/filters/registry); workspace canvas default `p-4`.
- **Applied:** Tasks, CRM (+ StatCard/CRMStats), Finance dashboard, AutomationDashboard, Agents overview, SEO KeywordExplorer + KeywordFilters.
- **Excluded:** global `Dashboard.tsx` per compliance gate.
- **Docs:** `.context/02_architecture_design.md` §3.3.1; `BUG_LEDGER` B077.
- **Verified:** `npm run build` frontend exit 0; `graphify update .` (6802 nodes).

## RankRay HQ — Structural architect execution pass (auditor VULs)

- **Schema/DTO:** `Deal.expectedCloseDate`, `Contact.lastName?`; CRM DTOs + deal service mapping; `UpdateExpenseDto` full optional fields; migration `20260604140000_structural_alignment_and_sanitization`.
- **State:** `crmStore`/`projectStore` rehydrate → `isLoading: true`; `refreshAll` always loads; `lastFetchedAt`; PDF `selectFeatureGateUi`; `isEntitlementsPending()` in `appNavigation`.
- **UI:** Finance `workspaceCanvasFlush`; Projects search row compact; pillar docs `.context/01,02,04,05` + B076/B077 ledger.
- **Verified:** `npx tsc -b` backend PASS; `npm run build` frontend PASS; `graphify update .` (6804 nodes).

## RankRay HQ — Critical re-alignment (Assets, cascade delete, finance skeleton)

- **Assets:** Restored live module — `AssetForm.tsx`, create/edit DOMAIN+HOSTING via API; removed coming-soon toasts; `moduleElementStyles` on filters.
- **Cascade delete:** `RelationalCascadeDeleteDialog` + `relationalDelete.ts`; HTTP 409 → checkbox gate → `?cascade=true` for CRM companies + Finance services/customers; backend cascade in `company.service` + `finance.service.deleteItem`.
- **Tenancy:** Documented workspace=tenant; `workspace-scope.ts` helpers; existing Prisma `tenant-scope` extension unchanged.
- **Finance UX:** `FinanceTabSkeleton` on tab switch; removed `mounted` blank gate.
- **Docs:** `.context/02` §3.3.2 + §4.1; `.context/05` sync table + §9.1.
- **Verified:** `npm run build` frontend PASS; `npx tsc -b` backend PASS; `graphify update .` (6823 nodes).

## RankRay HQ — Relational delete sweep (B079)

- **Graphify item relations:** `invoiceItem`, `quoteItem`, `recurringInvoiceItem`, `salesReceiptItem` all tied to finance service id.
- **Backend:** `relational-delete.helpers.ts`; `deleteItem` counts/cascades all line tables; contact/deal/project deletes use `ensure*` + scoped soft-delete + `?cascade=true`; projects no longer call heavy `findOne` before remove.
- **Frontend:** Cascade dialogs wired for Contacts, Pipeline, Projects (Companies/Customers/Items already had them); `crm.service`/`crmStore`/`projectStore` pass cascade flag.
- **Ledger:** `BUG_LEDGER` B079.
- **Verified:** `npx tsc -b` + `npm run build` PASS; `graphify update .` (6835 nodes).

## RankRay HQ — Instant-action optimistic CRUD

- **`optimisticMutation.ts` + `optimisticStoreHelpers.ts`:** apply/rollback/sync pattern; 409 rethrow for cascade dialogs.
- **Stores:** `crmStore` add/delete company; `financeStore` add/delete item; `taskStore` add/update/delete (no `isLoading` on create).
- **UI:** `Companies.tsx`, `Items.tsx`, `Tasks.tsx` — close modals immediately, `void` handlers, removed row delete spinners / bulk "Deleting..." locks.
- **Docs:** `.context/05` §19 instant-action CRUD contract.
- **Verified:** `npm run build` + `npx tsc -b` PASS.

## RankRay HQ — Platform-wide optimistic CRUD + cache-first SWR

- **Helpers:** `storeFetchPatterns.ts` (`cacheFirstListFetch`), `storeMutationPatterns.ts` (`optimisticPrepend/Patch/Remove`).
- **financeStore:** All finance list fetches cache-first; optimistic mutations for invoices, quotes, expenses, banks, payments, credit notes, receipts, recurring/retainers, customers, items; silent payment side-effects on sync success.
- **crmStore:** Cache-first fetches + `refreshAll({ forceLoader })`; optimistic contacts/deals/activities/company update; removed rehydrate `isLoading` flash.
- **projectStore:** Cache-first `fetchProjects`; optimistic project/task/time mutations; removed rehydrate loader.
- **taskStore:** Cache-first `fetchTasks`.
- **UI:** Finance tab skeleton only on cold dashboard cache; Contacts modal closes before `void` mutations.
- **Docs:** `docs/core/05_state_routing_ledger.md` §19 expanded (19.1 mutations + 19.2 SWR); `docs/core/02_architecture_design.md` finance skeleton note; references renumbered §20.
- **Verified:** `npm run build` frontend PASS; `npx tsc -b` backend PASS; `graphify update .` (6849 nodes).

## RankRay HQ — Finance guardrails + consolidation seal (2026-06-04)

- **Guardrail doc:** `projects/rankray-hq/docs/operations/finance_guardrails.md` (was `Ai Brain/memory/finance_guardrails.md`).
- **P0/P1 Finance:** Recurring engine (`RecurringInvoiceService` + hourly cron + generate API); Payments invoice picker; `useFinanceSectionBootstrap` on secondary tabs (no blank flash).
- **Git:** commit `a3473f393` on `dev` — clients hub stabilization, recurring engine, finance guardrails in repo docs (B083 BUG_LEDGER).
- **Verified:** `npx tsc -b`, `npm run build`, `graphify update .`, pushed `origin dev`.

## RankRay HQ — SSOT doc path migration (2026-06-04)

- Moved `.context/01`–`05` → `docs/core/` (canonical SSOT pillars); removed root `.context/`.
- Moved `Ai Brain/memory/finance_guardrails.md` → `docs/operations/finance_guardrails.md` (stub left in Ai Brain memory).
- Updated `.cursorrules`, `CLAUDE.md`, `docs/README.md`, `FILE_MAP.md`, `SSOT_LOCK.md`, `BUG_LEDGER.md`, cross-refs inside pillar docs.
- Agent rule: cross-reference `docs/core/` before architecture, multi-tenant, schema, or routing edits; new sub-modules → `docs/modules/` or update `05_state_routing_ledger.md`.
