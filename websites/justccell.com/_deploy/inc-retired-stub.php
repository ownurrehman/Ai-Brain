<?php
/**
 * RETIRED — all logic lives in wp-content/plugins/justccell-features/.
 *
 * Stale copies under justccell-theme/inc/ must stay empty so they cannot
 * redeclare functions alongside the plugin (product edit white-screen).
 *
 * @package Justccell
 */
declare(strict_types=1);

if (!defined('ABSPATH')) {
    exit;
}
